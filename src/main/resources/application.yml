
server:
    servlet:
        context-path: /
    port: 8888
spring:
    profiles:
        active: dev
    http:
        encoding:
            charset: utf-8
            enabled: true
            force: true


#mybatis:
#    typeAliasesPackage: cn.ff.burning.entity
#    mapperLocations: classpath*:mapper/*.xml
#    configuration:
#        map-underscore-to-camel-case: true

mybatis-plus:
    typeAliasesPackage: cn.ff.burning.entity
    mapperLocations: classpath*:mapper/*.xml
    configuration:
        map-underscore-to-camel-case: true
